# Secrets / env
.env
.env.*
.dev.vars
.dev.vars.*
*.key
*.pem
.a2a_token
A2A_SHARED_TOKEN*

# Local agent / session data
.claude/

# Dogfood (07) / prototype local artifacts
handoff.log
*.log
tunnel.txt
prototype/config.json
# generated, machine-specific (baked absolute fire.mjs path) — regenerate with gen-trigger.mjs
prototype/mcp/trigger/buildhud.schedules.ts
# hub runtime state (durable inbox) — local, not committed
prototype/hub/inbox.json

# Node
node_modules/
npm-debug.log*

# OS
.DS_Store

# Playwright MCP run artifacts (snapshots/screenshots from browser-worker e2e)
.playwright-mcp/
