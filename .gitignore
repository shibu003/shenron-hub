# Secrets / env
.env
.env.*
.dev.vars
.dev.vars.*
*.key
*.pem
.a2a_token
A2A_SHARED_TOKEN*

# Local agent / session data — ignore .claude contents, but SHARE the team hook + directive
.claude/*
!.claude/settings.json
!.claude/giogio-mcp-first.md

# Dogfood (07) / prototype local artifacts
handoff.log
*.log
tunnel.txt
prototype/config.json
# generated, machine-specific (baked absolute fire.mjs path) — regenerate with gen-trigger.mjs
prototype/mcp/trigger/buildhud.schedules.ts
# hub runtime state (durable inbox) — local, not committed
prototype/hub/inbox.json
# Wave: scheduler last-fired state — runtime, local only
prototype/hub/schedule-state.json
# Wave 11b: browser-control permission rules — runtime user choices (seed lives in code), local only
prototype/mcp/permissions.json

# Node
node_modules/
npm-debug.log*

# OS
.DS_Store

# Playwright MCP run artifacts (snapshots/screenshots from browser-worker e2e)
.playwright-mcp/
